package com.linktree.aibsc.web.dto.${InstanceName}s;

import com.linktree.aibsc.domain.${InstanceName}s.${DomainName};
import com.querydsl.core.annotations.QueryProjection;
import lombok.*;

@Builder
@Data
@NoArgsConstructor
public class ${DomainName}ResponseDto {
    private Long id;

 
}
